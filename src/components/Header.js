import './Header.css'

function Header(props) {

    return (
        <header>
            <div id="header-title">Where in the world?</div>
            <div id="theme-switch">
                {/* moon icon */}
            Dark Mode
            </div>
        </header>
    );
}

export default Header;